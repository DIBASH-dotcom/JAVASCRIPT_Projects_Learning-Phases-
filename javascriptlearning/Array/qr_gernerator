<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QR Genrator</title>
</head>
<body>
    <label> Name:</label>
    <input type="text" id="name" placeholder="Enter Your Name">

    <label> Last Name:</label>
    <input type="text" id="last" placeholder="Enter Your Last Name">

    <label for="dob"> Date Of Birth:</label>
    <input type="date" id="dob">

        <label> Age:</label>
        <input type="number" id="age" placeholder="Enter Your Age">

        <label> Phone Number:</label>
        <input type="number" id="Phonenumber"  placeholder="Enter Your Phone Number" oninput="validatePhoneNumber()">
        <span id="errorPhonenumber"></span>

        <button onclick="calculate()"> calculate</button>
<p id="result"></p>

    
    <script>

        function calculate(){

            let name= document.getElementById("name").value;
            let last=document.getElementById("last").value;
            let dob= document.getElementById("dob").value;
             let age= document.getElementById("age").value;
            let Phonenumber= document.getElementById("Phonenumber").value;
            let result= document.getElementById("result")
            let misssing=[];
            if(name ==="") {
                misssing.push("Name");
            }

            if(last ==="") {
                misssing.push("Last Name");
            }

        if(dob==="") {
            misssing.push("Date Of Birth ");
        }

        if(age==="") {
            misssing.push("Age")
        }
        if(Phonenumber==="") {
            misssing.push("Phone Number");
        }
    if(misssing.length>0) {
        result.innerHTML="Please Enter The Following fields:<br>" +misssing.join(",<br>");
        result.style.color="red";
        return;

    }
   

        }

        function validatePhoneNumber() {

            let phone =document.getElementById("PhoneNumber").value;

            let errorPhoneNumber= document.getElementById("errorPhonenumber").value;
            
if(phone.length>10) {
    error.innerText ="Phone NuMBERS mUST BE EXATLY 10 DIGITIS";
    error.innerText ="red";
    
} else {
    error.innerText =""
}


        }
    </script>
</body>
</html>
